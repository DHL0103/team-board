<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.promptech.springboottutorial.mapper.PostFileMapper">

    <insert id="saveFile" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post_files (post_id, original_name, stored_path, file_size, created_at)
        VALUES (#{postId}, #{originalName}, #{storedPath}, #{fileSize}, #{createdAt})
    </insert>

    <select id="findByPostId" resultType="kr.co.promptech.springboottutorial.model.PostFile">
        SELECT * FROM post_files WHERE post_id = #{postId}
    </select>

    <select id="findById" resultType="kr.co.promptech.springboottutorial.model.PostFile">
        SELECT * FROM post_files WHERE id = #{id}
    </select>

    <delete id="deleteById">
        DELETE FROM post_files WHERE id = #{id}
    </delete>

</mapper>
