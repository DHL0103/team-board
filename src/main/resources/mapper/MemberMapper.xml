<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.promptech.springboottutorial.mapper.MemberMapper">

    <select id="selectMemberById" resultType="kr.co.promptech.springboottutorial.model.Member">
        SELECT * FROM members WHERE id = #{id}
    </select>

    <select id="findByUsername" resultType="kr.co.promptech.springboottutorial.model.Member">
        SELECT * FROM members WHERE username = #{username}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM members WHERE username = #{username}
    </select>

    <insert id="insertMember" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO members (username, password, role)
        VALUES (#{username}, #{password}, #{role})
    </insert>

    <update id="updatePassword">
        UPDATE members SET password = #{password} WHERE id = #{id}
    </update>

    <select id="selectAllMembers" resultType="kr.co.promptech.springboottutorial.model.Member">
        SELECT * FROM members ORDER BY id
    </select>

    <update id="updateRole">
        UPDATE members SET role = #{role} WHERE id = #{id}
    </update>

</mapper>
